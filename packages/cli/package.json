{
  "name": "hyperiux",
  "version": "1.1.0",
  "description": "CLI for adding Hyperiux UI effects to your Next.js project. This package is a CLI installer only — it is not an importable JavaScript/TypeScript library.",
  "type": "module",
  "bin": {
    "hyperiux": "./src/index.js"
  },
  "exports": {
    "./package.json": "./package.json"
  },
  "files": [
    "src/commands",
    "src/utils",
    "src/index.js",
    "README.md",
    "LICENSE",
    "scripts/smoke-packed-package.mjs"
  ],
  "scripts": {
    "build": "node --check src/index.js && node --check src/commands/add.js && node --check src/commands/init.js && node --check src/commands/list.js && node --check src/commands/login.js && node --check src/commands/outdated.js && node --check src/commands/versions.js && node --check src/commands/diff.js && node --check src/utils/auth.js && node --check src/utils/config.js && node --check src/utils/package-manager.js && node --check src/utils/registry.js && node --check src/utils/lockfile.js && node --check src/utils/semver.js",
    "lint": "eslint src",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "smoke:bin": "node src/index.js --help",
    "smoke:pack": "node scripts/smoke-packed-package.mjs",
    "pack:dry": "npm pack --dry-run",
    "prepublishOnly": "npm run lint && npm run test && npm run build && npm run smoke:bin && npm run pack:dry"
  },
  "dependencies": {
    "chalk": "^5.4.1",
    "commander": "^13.1.0",
    "diff": "^9.0.0",
    "ora": "^8.1.1",
    "prompts": "^2.4.2"
  },
  "keywords": [
    "hyperiux",
    "ui",
    "effects",
    "animations",
    "cli",
    "components",
    "nextjs",
    "gsap",
    "three",
    "webgl"
  ],
  "author": "Hyperiux",
  "license": "MIT",
  "homepage": "https://vault.hyperiux.com",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Hyperiux-Immersion-Labs/hyperiux-components.git",
    "directory": "packages/cli"
  },
  "bugs": {
    "url": "https://github.com/Hyperiux-Immersion-Labs/hyperiux-components/issues"
  },
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "eslint": "^9",
    "vitest": "^4.1.8"
  }
}
